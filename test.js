const duble = ()=>{
    let numbers = [1,2,3]
    let newnumbers = []
    let letters =[a,b,c]
    let n = 5
    for(number of numbers ){
        let newnumber = number * 2;
        newnumbers.push(newnumber);
        let newlerrer = 
        newletters.push()
    }
    return [newnumbers, n];
}
console.log(duble()[0])
